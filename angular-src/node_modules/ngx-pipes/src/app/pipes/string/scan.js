"use strict";
var core_1 = require('@angular/core');
var helpers_1 = require('../helpers/helpers');
var ScanPipe = (function () {
    function ScanPipe() {
    }
    ScanPipe.prototype.transform = function (text, args) {
        if (args === void 0) { args = []; }
        return helpers_1.isString(text)
            ? text.replace(/\{(\d+)}/g, function (match, index) { return !helpers_1.isUndefined(args[index]) ? args[index] : match; })
            : text;
    };
    ScanPipe.decorators = [
        { type: core_1.Pipe, args: [{ name: 'scan' },] },
    ];
    /** @nocollapse */
    ScanPipe.ctorParameters = function () { return []; };
    return ScanPipe;
}());
exports.ScanPipe = ScanPipe;
//# sourceMappingURL=scan.js.map